# 🚀 Деплой на Amvera для secrethouse.by

## Подготовка проекта

Проект уже настроен для деплоя на Amvera со следующими файлами:

- ✅ **`Dockerfile`** - Multi-stage сборка (Node.js + Nginx)
- ✅ **`.dockerignore`** - Оптимизация размера образа
- ✅ **`nginx.conf`** - Конфигурация веб-сервера
- ✅ **`amvera.yml`** - Конфигурация Amvera

## Шаги деплоя

### 1. Подготовка репозитория

```bash
# Убедитесь что все изменения закоммичены
git status

# Закоммитьте если есть изменения
git add .
git commit -m "Prepare for Amvera deployment"

# Запушьте в GitHub/GitLab
git push origin main
```

### 2. Создание проекта на Amvera

1. Зайдите на [amvera.ru](https://amvera.ru)
2. Авторизуйтесь через GitHub/GitLab
3. Нажмите **"Создать проект"**
4. Выберите ваш репозиторий `secret-house-web`
5. Amvera автоматически обнаружит `amvera.yml` конфигурацию

### 3. Настройка переменных окружения

В панели управления Amvera добавьте следующие **Build-time** переменные окружения:

```env
VITE_LOCATION_LAT=53.934202
VITE_LOCATION_LON=27.300385
VITE_LOCATION_ADDRESS=Минск, Беларусь
VITE_BOT_USERNAME=the_secret_house_booking_bot
VITE_TELEGRAM_ADMIN=the_secret_house
VITE_TELEGRAM_CHANNEL=sekret_blr
VITE_INSTAGRAM_USERNAME=private_sekret_blr
VITE_PHONE_NUMBER=+375257908378
```

**Важно:** Все переменные с префиксом `VITE_` должны быть добавлены как **Build-time** переменные, так как они встраиваются в приложение во время сборки.

### 4. Настройка домена

1. В настройках проекта перейдите в раздел **"Домены"**
2. Добавьте домен: **`secrethouse.by`**
3. Добавьте алиас: **`www.secrethouse.by`**
4. Следуйте инструкциям Amvera для настройки DNS записей:
   - Добавьте A-запись для `secrethouse.by` → IP от Amvera
   - Добавьте CNAME-запись для `www.secrethouse.by` → домен от Amvera

### 5. Запуск деплоя

1. Нажмите **"Задеплоить"**
2. Amvera автоматически:
   - Склонирует репозиторий
   - Соберет Docker образ используя `Dockerfile`
   - Запустит контейнер на порту 80
   - Настроит SSL сертификат (Let's Encrypt)
   - Привяжет ваш домен

### 6. Проверка деплоя

После успешного деплоя:

```bash
# Проверьте health endpoint
curl https://secrethouse.by/health

# Откройте в браузере
# https://secrethouse.by
```

## Конфигурация проекта

### amvera.yml

```yaml
meta:
  environment: docker      # Использовать Docker окружение
  toolchain:
    name: docker
build:
  dockerfile: Dockerfile   # Путь к Dockerfile
  skip: false             # Не пропускать сборку
run:
  containerPort: "80"     # Порт контейнера
```

### Dockerfile

Двухэтапная сборка:

1. **Stage 1 (Builder):**
   - Node.js 20 Alpine
   - Установка зависимостей (`npm ci`)
   - Сборка приложения (`npm run build`)
   - Выход: `/app/dist`

2. **Stage 2 (Production):**
   - Nginx Alpine
   - Копирование собранных файлов
   - Настройка Nginx для SPA
   - Порт 80

### nginx.conf

Конфигурация включает:

- ✅ **SPA routing** - перенаправление всех роутов на `index.html`
- ✅ **Gzip сжатие** - оптимизация размера файлов
- ✅ **Кеширование** - статика 1 год, HTML 1 час
- ✅ **Security headers** - защита от XSS, clickjacking
- ✅ **Health check** - endpoint `/health`

## Обновление проекта

Для обновления приложения:

```bash
# 1. Внесите изменения в код
# 2. Закоммитьте
git add .
git commit -m "Update: описание изменений"

# 3. Запушьте
git push origin main

# Amvera автоматически пересоберет и задеплоит новую версию
```

### Ручной редеплой

Если автодеплой отключен:

1. Зайдите в панель Amvera
2. Откройте ваш проект
3. Нажмите **"Пересобрать"** или **"Редеплой"**

## Мониторинг и логи

### Просмотр логов

В панели Amvera:

1. Откройте ваш проект
2. Перейдите в раздел **"Логи"**
3. Выберите:
   - **Build logs** - логи сборки
   - **Runtime logs** - логи Nginx

### Метрики

В разделе **"Метрики"** доступны:

- CPU usage
- Memory usage
- Network traffic
- Request count

### Health Check

Amvera автоматически проверяет доступность на порту 80. Дополнительно доступен endpoint:

```bash
curl https://secrethouse.by/health
# Ответ: healthy
```

## Troubleshooting

### Проблема: Build failed

**Решение:**
1. Проверьте Build logs в панели Amvera
2. Убедитесь что все переменные окружения установлены
3. Проверьте что `.env` файл в `.gitignore` (не должен быть в репозитории)

### Проблема: Container не запускается

**Решение:**
1. Проверьте Runtime logs
2. Убедитесь что порт 80 указан в `amvera.yml`
3. Проверьте что Nginx конфигурация корректна

### Проблема: 404 на роутах React Router

**Решение:**
Убедитесь что в `nginx.conf` настроен SPA routing:

```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

### Проблема: Переменные окружения не работают

**Решение:**
1. Все переменные с префиксом `VITE_` должны быть **Build-time**
2. После изменения переменных нужен **пересборка** (rebuild)
3. Runtime переменные не работают для Vite приложений

## Оптимизация

### Размер образа

Текущий образ оптимизирован:
- ✅ Multi-stage build (только production артефакты)
- ✅ Alpine Linux (минимальный размер)
- ✅ `.dockerignore` (исключение node_modules)

### Скорость сборки

Для ускорения сборки:
- ✅ `npm ci` вместо `npm install`
- ✅ Кеширование слоев Docker
- ✅ Копирование `package.json` перед исходниками

### Производительность

Nginx настроен для максимальной производительности:
- ✅ Gzip сжатие
- ✅ Агрессивное кеширование статики
- ✅ `sendfile` и `tcp_nopush`
- ✅ Worker connections: 1024

## Безопасность

### Заголовки безопасности

Настроены в `nginx.conf`:

```nginx
X-Frame-Options: SAMEORIGIN
X-Content-Type-Options: nosniff
X-XSS-Protection: 1; mode=block
Referrer-Policy: no-referrer-when-downgrade
```

### SSL/TLS

Amvera автоматически:
- ✅ Выпускает SSL сертификат (Let's Encrypt)
- ✅ Обновляет сертификат
- ✅ Перенаправляет HTTP → HTTPS

### Защита от атак

- ✅ Скрытие версии Nginx
- ✅ Блокировка доступа к скрытым файлам (`.`)
- ✅ Rate limiting (настраивается в Amvera)

## Стоимость

Amvera предлагает:

- **Free tier** - для тестирования
- **Paid plans** - для production

Ваш проект использует:
- 1 контейнер
- Порт 80
- SSL сертификат (бесплатно)

## Полезные ссылки

- [Документация Amvera](https://amvera.ru/docs)
- [Поддержка Amvera](https://amvera.ru/support)
- [Статус сервисов Amvera](https://status.amvera.ru)

## Контакты проекта

- **Домен:** https://secrethouse.by
- **Telegram:** @the_secret_house
- **Instagram:** @private_sekret_blr

---

✅ **Проект готов к деплою на Amvera!**
